{
	"working_dir": "$file_path",
    "shell_cmd": "gcc -Wall -std=c11 ${file} -o ${file_path}/${file_base_name} && start cmd /c ${file_path}/${file_base_name}",
    "working_dir": "${file_path}",
    "selector": "source.c",
}

// Dùng getch() để xem kết quả