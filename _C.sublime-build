{
	"working_dir": "$file_path",
    "shell_cmd": "gcc -Wall -std=c11 ${file} -o ${file_base_name} && cmd /c ${file_path}/${file_base_name}",
    "working_dir": "${file_path}",
    "selector": "source.c",
}
/*
chekc version C(cmd): gcc -v
c++ -v
No run if scanf or getch() =>> Die process
*/